# إعدادات الشبكة والمنافذ
network_enr_address = "0.0.0.0"
network_libp2p_port = 1234
network_discovery_port = 1234
rpc_listen_address = "0.0.0.0:5678"

# الربط بنقاط الوصول السريعة (تأكد من استقرار هذا الرابط)
blockchain_rpc_endpoint = "https://0g-json-rpc-public.0glabs.ai"

# إعدادات العقد الذكي (هذه العناوين ثابتة لشبكة التيست نت الحالية)
log_contract_address = "0x8888888888888888888888888888888888888888"
log_sync_start_block_number = 800000

# مسار قاعدة البيانات (سيتم تخزينه في الذاكرة المؤقتة لسرعة الأداء)
db_dir = "db"
